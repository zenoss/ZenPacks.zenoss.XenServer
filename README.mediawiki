== TODOs ==

Tracking tasks here at least until the first revision is done.

;Musts
# Tunable Collection Intervals:
#* zXenServerPerfInterval
#* zXenServerModelInterval
#* zXenServerEventsInterval
# External Impact adapters.
# CloudStack Impact adapters.
# Documentation.

;Niceties
# Add useful information to device overview screen.
# zProperty to disable modeling of VMs, VBDs, VIFs and vApps.
# Vocabulary for XenServerRRDDataPointInfo *_aggregation properties.
# Add bond interfaces. (Bond)
# Add VLANs. (VLAN)
# Fix sleep(5) workaround in add_xenserver facade method.
# Screencast.

== Scratch Pad ==

Tidbits that are useful to me while developing the ZenPack.

;Add the XenServer endpoint.
<pre>
d = dmd.Devices.XenServer.createInstance('test-xenserver-pool')
d.setPerformanceMonitor('localhost')
d.setZenProperty('zXenServerAddresses', ['192.168.66.71'])
d.setZenProperty('zXenServerUsername', 'root')
d.setZenProperty('zXenServerPassword', 'zenoss')
commit()

import time; time.sleep(4)
d.collectDevice()

sync()
d.setZenProperty('zCollectorLogChanges', True)
commit()
</pre>
